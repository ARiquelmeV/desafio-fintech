const financialData = {
    symbol: "",       // Símbolo como cadena (puede inicializarse vacío o con un valor predeterminado)
    yearStart: 0,    
    yearEnd: 0,   
    closePricesStart: 0,   
    closePricesEnd: 0, 
    yield: 0.0,        // Rendimiento (yield) como número decimal (inicializado en 0.0)
    ETFAmount: 0.0,     // Cantidad de tokens del ETF 
  };
  
  export default financialData;
  